/**
 * Public site settings.
 *
 * Set `appStoreId` to the numeric Apple App Store ID from App Store Connect
 * (the digits after `id` in the listing URL). The download badge then points
 * at https://apps.apple.com/app/id{appStoreId}.
 *
 * Optional `supportEmail` is shown on the Support section and used for mailto
 * links. Leave it blank to keep support self-serve via the FAQ.
 */
window.BAC_BUDDY_CONFIG = {
  appStoreId: "",
  supportEmail:"bacbuddy.app@gmail.com",
};
